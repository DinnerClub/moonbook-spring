<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper
	namespace="net.mooncloud.moonbook.repository.payment.UserPaymentDailySubtotalDao">

	<insert id="save" parameterType="UserPaymentDailySubtotal"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO
		`user_payment_daily_subtotal`
		(`userid`, `thedate`, `year`,
		`month`,
		`day`, `money`,
		`syn`, `created`,
		`updated`, `status`)
		VALUES (
		#{userid},
		#{thedate},
		#{year},
		#{month},
		#{day},
		#{money},
		#{syn},
		#{created},
		#{updated},
		#{status}
		)
		ON DUPLICATE KEY
		UPDATE
		year=VALUES(year),month=VALUES(month),day=VALUES(day),money=VALUES(money),syn=VALUES(syn),updated=VALUES(updated),status=VALUES(status)
	</insert>

	<insert id="incrementalSave" parameterType="UserPaymentDailySubtotal"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO
		`user_payment_daily_subtotal`
		(`userid`, `thedate`, `year`,
		`month`,
		`day`, `money`,
		`syn`, `created`,
		`updated`, `status`)
		VALUES (
		#{userid},
		#{thedate},
		#{year},
		#{month},
		#{day},
		#{money},
		#{syn},
		#{created},
		#{updated},
		#{status}
		)
		ON DUPLICATE KEY
		UPDATE
		money=money+VALUES(money),syn=VALUES(syn),updated=VALUES(updated)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="Long">
		DELETE FROM
		`user_payment_daily_subtotal`
		where userid=#{0} AND thedate=#{1}
	</delete>

	<update id="update" parameterType="UserPaymentDailySubtotal">
		UPDATE
		`user_payment_daily_subtotal`
		SET `year`=#{year}, `month`=#{month},
		`day`=#{day},
		`money`=#{money}, `syn`=#{syn},
		`updated`=#{updated},
		`status`=#{status} WHERE
		(`userid`=#{userid} AND thedate=#{thedate})
	</update>
</mapper> 
